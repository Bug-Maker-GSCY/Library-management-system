#include <iostream>
#include <string>
using namespace std;

class AuthenticationService : public User
{
private:
	AuthenticationService AuthenticationService;
public:
	void login(account, password);
	bool signout()
	void newUser(account, password)
};
