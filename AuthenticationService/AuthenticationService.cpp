#include "Authentication.h"


void AuthenticationService::login(account, password)
{
	cout << "Enter your account:" << endl;
   	cin >> account_get;
  	cout << "Enter your password:" << endl;
  	cin >> password_get;
	if account_get = library.account || password_get = library.password;
	cout << "Login Successful!";
	else
	cout << "Login failed, please check account and password."
  	return 0;
}

bool AuthenticationService::signout()
{
	exit(0);
}

void

void AuthenticationService::newUser(account, password)
{
   string username;
   string password;
   cout << "Account created!" << endl;
}
